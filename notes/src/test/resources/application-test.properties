# Profil de test : l'URI Mongo est fournie dynamiquement par le conteneur Testcontainers
# via @ServiceConnection (pas de serveur Mongo a demarrer a la main). On garde la creation
# d'index active pour rester fidele au comportement runtime.
spring.data.mongodb.auto-index-creation=true
